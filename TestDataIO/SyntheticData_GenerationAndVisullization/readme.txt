产生合成文档数据集，并使用MATLAB进行可视化化，保存成相应的TXT格式，使用c++进行读取，并尝试使用CUDA核进行简单的计算，如统计词频（利用先验作为权重），然后写回txt中使用MATLAB进行可视化：

1.明确合成数据集的形式，先验格式，程序应该能找到现成的
2.直接用MATLAB进行可视化程序，以TXT做输入；这个程序要做成两段，一段是统计频谱，一段是对频谱进行显示，接下来单独用到后者；
3.c++对应进行读取文档；
4.串行统计每篇文档的词频；
5.并行统计词频；
6.对统计之后的频谱进行可视化，对比效果，以验证是否正确；
7.将统计词频的并行放在MATLAB中尝试，然后直接可视化